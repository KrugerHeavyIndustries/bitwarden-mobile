<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Bit.App.Pages.EnvironmentPage"
    xmlns:pages="clr-namespace:Bit.App.Pages"
    xmlns:u="clr-namespace:Bit.App.Utilities"
    x:DataType="pages:EnvironmentPageViewModel"
    Title="{Binding PageTitle}">

    <ContentPage.BindingContext>
        <pages:EnvironmentPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{u:I18n Cancel}" Clicked="Close_Clicked" Order="Primary" Priority="-1" />
        <ToolbarItem Text="{u:I18n Save}" Command="{Binding SubmitCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout Spacing="20">
            <StackLayout StyleClass="box">
                <StackLayout StyleClass="box-row-header">
                    <Label Text="{u:I18n SelfHostedEnvironment, Header=True}"
                           StyleClass="box-header, box-header-platform" />
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n ServerUrl}"
                        StyleClass="box-label" />
                    <Entry
                        Text="{Binding BaseUrl}"
                        Keyboard="Url"
                        Placeholder="ex. https://bitwarden.company.com"
                        StyleClass="box-value"
                        ReturnType="Go"
                        ReturnCommand="{Binding SubmitCommand}" />
                </StackLayout>
                <Label
                    Text="{u:I18n SelfHostedEnvironmentFooter}"
                    StyleClass="box-footer-label" />
            </StackLayout>
            <StackLayout StyleClass="box">
                <StackLayout StyleClass="box-row-header">
                    <Label Text="{u:I18n CustomEnvironment, Header=True}"
                           StyleClass="box-header, box-header-platform" />
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n WebVaultUrl}"
                        StyleClass="box-label" />
                    <Entry
                        x:Name="_webVaultEntry"
                        Text="{Binding WebVaultUrl}"
                        Keyboard="Url"
                        StyleClass="box-value" />
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n ApiUrl}"
                        StyleClass="box-label" />
                    <Entry
                        x:Name="_apiEntry"
                        Text="{Binding ApiUrl}"
                        Keyboard="Url"
                        StyleClass="box-value" />
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n IdentityUrl}"
                        StyleClass="box-label" />
                    <Entry
                        x:Name="_identityEntry"
                        Text="{Binding IdentityUrl}"
                        Keyboard="Url"
                        StyleClass="box-value" />
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n IconsUrl}"
                        StyleClass="box-label" />
                    <Entry
                        x:Name="_iconsEntry"
                        Text="{Binding IconsUrl}"
                        Keyboard="Url"
                        StyleClass="box-value"
                        ReturnType="Go"
                        ReturnCommand="{Binding SubmitCommand}" />
                </StackLayout>
                <Label
                    Text="{u:I18n CustomEnvironmentFooter}"
                    StyleClass="box-footer-label" />
            </StackLayout>
            <StackLayout StyleClass="box">
                <StackLayout StyleClass="box-row-header">
                    <Label Text="{u:I18n ClientAuthentication, Header=True}"
                           StyleClass="box-header, box-header-platform" />
                </StackLayout>
                <StackLayout StyleClass="box">
                    <StackLayout StyleClass="box-row, box-row-switch">
                        <Switch
                            IsToggled="{Binding UseClientAuthentication}"
                            StyleClass="box-value"
                            HorizontalOptions="Start"
                            Margin="0, 0, 10, 0"/>
                        <Label StyleClass="box-footer-label"
                           HorizontalOptions="Fill">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="{u:I18n UseClientAuthentication}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                     </StackLayout>
                 </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n ClientAuthenticationCertificate}"
                        StyleClass="box-label" />
                    <StackLayout StyleClass="box-row, box-row-input">
                        <Editor
                            x:Name="_clientCertificateEditor"
                            AutoSize="TextChanges"
                            StyleClass="box-value"
                            Text="{Binding ClientCertificatePem}"
                            IsSpellCheckEnabled="false"
                            IsTextPredictionEnabled="false"
                            HeightRequest="180">
                        </Editor>
                    </StackLayout>
                </StackLayout>
                <StackLayout StyleClass="box-row">
                    <Label
                        Text="{u:I18n ClientAuthenticationPrivateKey}"
                        StyleClass="box-label" />
                    <StackLayout StyleClass="box-row, box-row-input">
                        <Editor
                            x:Name="_clientPrivateKeyEditor"
                            AutoSize="TextChanges"
                            StyleClass="box-value"
                            Text="{Binding ClientPrivateKeyPem}"
                            IsSpellCheckEnabled="false"
                            IsTextPredictionEnabled="false"
                            HeightRequest="180">
                        </Editor>
                    </StackLayout>
                </StackLayout>
                  <Label
                    Text="{u:I18n CustomAuthenticationFooter}"
                    StyleClass="box-footer-label" />
            </StackLayout>
        </StackLayout>
    </ScrollView>

</pages:BaseContentPage>
